{
  "name": "beauty-shop",
  "version": "1.0.0",
  "description": "Beauty Shop - MedusaJS e-commerce platform",
  "private": true,
  "scripts": {
    "validate-env": "node scripts/validate-env.js",
    "health-check": "node scripts/health-check.js",
    "dev:backend": "cd beauty-shop && npm run dev",
    "dev:storefront": "cd beauty-shop-storefront && npm run dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:storefront\"",
    "migrate": "cd beauty-shop && npx medusa db:migrate",
    "typecheck": "npm run typecheck --prefix beauty-shop && npm run typecheck --prefix beauty-shop-storefront",
    "lint": "npm run lint --prefix beauty-shop && npm run lint --prefix beauty-shop-storefront",
    "build": "npm run build --prefix beauty-shop && npm run build --prefix beauty-shop-storefront",
    "test": "(npm run test --prefix beauty-shop || true) && (npm run test --prefix beauty-shop-storefront || true)",
    "secrets:check": "node scripts/validate-env.js && git ls-files | grep -qE '^\\.env' && exit 1 || exit 0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
